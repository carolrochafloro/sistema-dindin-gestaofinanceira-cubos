const newTransaction = require("./newTransaction");
const listTransactions = require("./listTransactions");
const listCategories = require("./listCategories");

module.exports = { newTransaction, listTransactions, listCategories };
